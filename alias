h	history
o	xdg-open
t	echo test
hoover	make clean && git reset --hard origin/master && rm -f config.h
emoji	cat ~/.scripts/.emoji | dmenu | awk '{print $1}'
grep	grep --color=auto
diff	diff --color=auto
vi	nvim
